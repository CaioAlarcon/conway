#include "viewgame.h"
#include "cultura.h"
#include <stdio.h>

int main(){
    
    cultura cult(64,64);
    while (true){
        
        
        sf::Time refresh = sf::seconds(8.8f);
        sf::sleep (refresh);
}


    return 0;
}
